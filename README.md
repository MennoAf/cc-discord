# claude-discord-bridge

Localhost HTTP bridge between Claude Code sessions and Discord. See docs/ for design.

Install with `uv tool install .` to place the CLI at `~/.local/bin/claude-discord-bridge`, which matches the systemd unit template at `packaging/claude-discord-bridge.service`.
